<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div class="container">
        <button type="button" class="btn btn-success">Generate Now</button>
    </div>

    <script>
        document.querySelector(".container>button").addEventListener("click", async () => {
            console.log("Yes")
            let a = await fetch("/generate")
            let r = await a.json()  // This will now work because we send JSON
            console.log(r)
        })
    </script>
</body>

</html>